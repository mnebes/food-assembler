{
  "name": "food-assembler",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun run src/index.ts build",
    "crawl": "bun run src/index.ts crawl",
    "dev": "bun run --watch src/index.ts build",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "cheerio": "^1.2.0",
    "playwright": "^1.61.1",
    "unpdf": "^1.6.2"
  }
}
